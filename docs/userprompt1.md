현재 기능을 바탕으로 다음 업데이트를 하려고 한다. 이를 위해 영어로 체크리스트 형식으로 구체화하여 작성해서, newchecklist2.md파일로 저장하라. 이때, 기존의 기능을 토대로 구체화 및 검토하여 엄밀히 작성하라. 해당 md 파일의 목적은 기능 구현을 위한 상세한 플랜 겸 체크리스트이다.(추가요청:## live2d 관련 기본 정리 및 보완의 내용은 직접 구체화 및 현재 코드를 보고 보강하라.)
또한 아래 내용은 구체적이지 않으며, 미완성이다. 완성해서 작동되게 보완하라. 
기본적으로 live2d 기능 고급 설정의 파라미터 조절은 현재 작동되는 바 없으며, 모델 별로 파라미터 변수가 다르기에 주의해야 한다.


# live2d 기능 구체화 및 보강
live2d 설정의 고급 설정에 다음 버튼을 추가한다:
## live2d 기능 테스트
live2d 기능과 관련된 직접 설정 및 테스트를 할 수 있으며 해당 공간에는 2개의 탭을 둔다.
### 파라미터 실제 조절
조정 가능 파라미터를 전부 보여주고, 드래그로 조절 할 수 있게 한다. 이떄 파라미터 기능 이름은 영어 파라미터 변수 값 명을 그대로 표기한다.
이때 조절 값은 실제 live2d에 반영된다. 또한 상단에 reset 버튼을 추가하여 모든 파라미터를 초기화 할 수 있도록 한다.

### 명령어 입력
채팅 ui을 두고, 여기서 ai가 입력하는 xml명령어를 직접 쳐서 작동여부를 확인할 수 있다.(실시간)

### 모션 추가
상단에 api 프리셋을 선택할 수 있게 하고, 그 밑에 프롬프트 입력창을 두어, 해당 공간에서 해당 api로 채팅을 할 수 있게 한다. 이때, 이 api에 권한을 부여하여, json 파일을 수정, 추가할 수 있도록 두어라.
기본 프롬프트로는 영어로, 적당히 llm이 어떤 모션 프리셋을 만들어 json으로 두고, 이를 활용 가능 모션으로 저장하게 한다.
해당 공간은 단, 채팅 공간만 있으며, api 호출시 mcp 느낌으로 작동하는 특이사항만 있다.

## live2d llm 조절 기능 보완 및 구체화
live2d의 llm 조절은 다음 방식으로 실행되어야 한다.
api 호출 -> api 답변 -> response 단계에서 작동하는 lua 함수 수준에서 xml 명령어를 시행한다.-> 이후 해당 함수 수준에서 시행된 xml 명령어를 제거한다 -> 메인 채팅 세션에 필터된 답변이 보내진다. -> 알림 기능이 작동해야할 환경이면 알림으로 해당 답변이 전달된다.

## 명령어 구체화
live2d 모션은 다음과 같이 실현된다.
- llm의 xml 명령어를 읽어 차례대로 수행
- 이때 명령어에 n초를 기다리는 명령어가 있어야 하며, 부드러운 모션을 구현하기 위해 파라미터 값을 어떤 값으로 천천히 바꾸는 명령어가 있어야한다. 또한 기존에 구현된 모션을 실행하는 명령어를 포함한다.
- 파라미터 조절 명령어를 위해, 고유한 파라미터 명을 미리 지정하지 않고, parameterN과 같이 일반화 하여 저장해둔다. 이후 